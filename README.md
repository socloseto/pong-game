# Pong Game — C++ & SFML Implementation

### Короткий опис реалізації згідно технічного завдання<br>
  1. Використовуються константи, класи `enum`, `static_cast`, повністю відсутнє ручне керування пам'яттю, об'єкти передаються за посиланням, шаблонні функції з темплейтами<br>
  2. Запроваджена ієрахія класів з базовими класами, які мають віртуальні методи та деструктори, інкапсульовані змінні та методи які приховують свою логіку, перевизначені методи від базового класу<br>
  3. Використано  `std::vector` для списку обсерверів та скінів та `std::map` для кешування скінів, використано розумні покажчики при створенні нових об'єктів <br>
  4. Код відформатовано за допомогою clang-format<br>
  5. (немає?)<br>
  6. Спочатку було підключено динамічну бібліотеку **sfml x64-windows**, далі в цілях пакування ассетів гри та бібліотек без яких гра не запуститься в один `.exe` файл було перелінковано на **sfml x64-windows-static**<br>
  7. Структуровано розділення логіки на обробку подій, логіки та фізики, та відмальовування в файлі `Game.cpp` метод `run` запускає спрощені 3 методи за категоріями задач. Все це працює в комбінації з `while(window.isOpen())` що і називають ігровий цикл<br>
  8. Першим було використано **factory** для динамічного створення скінів мяча, наступним було використано **observer**, де панель рахунку дізнається від мяча про гол, останнім  реалізовано **singleton** 😁😱, для управління ресурсами аудіо плеєра<br>
  9. **[pong.exe](https://github.com/socloseto/pong-game/releases/download/v1.0.0/pong.exe)**<br>
  10. Readme так readme. Даний документ є звітністю по тестовому завданню.<br>
